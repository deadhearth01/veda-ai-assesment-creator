steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -f
      - apps/web/Dockerfile
      - --build-arg
      - NEXT_PUBLIC_API_URL=${_API_URL}
      - --build-arg
      - NEXT_PUBLIC_WS_URL=${_API_URL}
      - -t
      - asia-south1-docker.pkg.dev/$PROJECT_ID/vedaai/web:latest
      - .
images:
  - asia-south1-docker.pkg.dev/$PROJECT_ID/vedaai/web:latest
timeout: 1200s
